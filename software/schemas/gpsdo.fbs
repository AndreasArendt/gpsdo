namespace gpsdo;

table Status {  
  phase_cnt: float;
  freq_error_hz: float;
  freq_drift_hz_s: float;  
  voltage_control_v: float;
  voltage_measured_v: float;
  temperature_c: float;
  raw_counter_value : uint;
}

root_type Status;